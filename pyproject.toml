[project]
name = "orderservice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-json-logger>=4.0.0",
    "ruff>=0.15.6",
]
